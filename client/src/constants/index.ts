export enum AUTH_ROUTE_PATHS {
  ADMIN = '/admin',
  BASKET = '/basket',
}

export enum PUBLIC_ROUTE_PATHS {
  SHOP = '/',
  LOGIN = '/login',
  REGISTRATION = '/registration',
  DEVICE = '/device',
}
